# Optional. If set, the agent uses Claude to phrase grounded answers more
# naturally. If left unset, the agent still works fully offline using
# extractive answers pulled directly from the approved source documents.
ANTHROPIC_API_KEY=
